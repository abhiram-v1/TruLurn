# supabase/ - Archived Database Artifacts

This folder is archive-only. TruLurn MVP now uses MongoDB Atlas + NextAuth as the active database/auth path.

Do not apply these migrations for the MVP unless the project explicitly reopens a Supabase migration later.

The historical SQL files are retained only as schema reference from an earlier architecture direction.
